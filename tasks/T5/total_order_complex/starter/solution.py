def compare_complex(a: complex, b: complex) -> int:
    """Define a total ordering on complex numbers that preserves
    addition and multiplication. Returns negative if a < b,
    zero if a == b, positive if a > b."""
    pass
