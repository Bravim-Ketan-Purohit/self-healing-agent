def parse(grammar: str, input_str: str) -> bool:
    """Parse input_str according to the given context-free grammar.
    Returns True if the string is in the language, False otherwise.
    Must run in O(n) time for any CFG."""
    pass
